{
  "name": "@jade/api",
  "version": "1.0.0",
  "description": "The API powering the application.",
  "main": "index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "check-types": "tsc -p tsconfig.json --noEmit",
    "format": "prettier -w .",
    "format:check": "prettier --check .",
    "lint": "eslint . --fix",
    "lint:check": "eslint .",
    "test": "vitest run"
  },
  "keywords": [
    "api",
    "backend"
  ],
  "author": "clairechabas",
  "license": "ISC",
  "devDependencies": {
    "@jade/eslint-config": "workspace:*",
    "@jade/typescript-config": "workspace:*",
    "@types/node": "^25.0.3",
    "tsx": "^4.21.0",
    "vitest": "^4.0.16"
  },
  "dependencies": {
    "@jade/api-client": "workspace:*",
    "fastify": "^5.6.2"
  }
}
