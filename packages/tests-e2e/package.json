{
  "name": "@jade/tests-e2e",
  "version": "1.0.0",
  "description": "E2E tests to check user flows and critical paths.",
  "private": true,
  "scripts": {
    "check-types": "tsc -p tsconfig.json --noEmit",
    "format": "prettier -w .",
    "format:check": "prettier --check .",
    "lint": "eslint . --fix",
    "lint:check": "eslint .",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "keywords": [],
  "author": "clairechabas",
  "license": "MIT",
  "devDependencies": {
    "@jade/eslint-config": "workspace:*",
    "@jade/typescript-config": "workspace:*",
    "@playwright/test": "^1.57.0"
  }
}
